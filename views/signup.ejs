<!DOCTYPE html>
<html>
<head>
  <title>Register - SocialApp</title>
  <link rel="manifest" href="/manifest.json">
  <meta name="theme-color" content="#1a1a2e">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
  <style>
    * { margin:0; padding:0; box-sizing:border-box; font-family:'Poppins',Arial,sans-serif; }
    body { background:linear-gradient(135deg,#0f0c29,#302b63,#24243e); min-height:100vh; display:flex; align-items:center; justify-content:center; padding:20px; }
    .card { background:rgba(255,255,255,0.07); backdrop-filter:blur(20px); border-radius:24px; padding:40px 28px; width:100%; max-width:380px; border:1px solid rgba(255,255,255,0.1); }
    .logo-circle { width:90px; height:90px; border-radius:50%; background:white; border:3px solid #6a11cb; display:flex; align-items:center; justify-content:center; margin:0 auto 16px; font-size:40px; }
    .app-name { text-align:center; color:white; font-size:26px; font-weight:800; margin-bottom:4px; }
    .app-tagline { text-align:center; color:#a78bfa; font-size:12px; letter-spacing:3px; text-transform:uppercase; margin-bottom:30px; }
    .error { background:rgba(220,53,69,0.2); color:#ff6b6b; padding:10px; border-radius:10px; margin-bottom:15px; font-size:13px; text-align:center; }
    .input-group { background:rgba(255,255,255,0.08); border-radius:14px; padding:14px 18px; margin-bottom:14px; display:flex; align-items:center; gap:12px; border:1px solid rgba(255,255,255,0.1); }
    .input-group input { background:none; border:none; outline:none; color:white; font-size:15px; width:100%; }
    .input-group input::placeholder { color:rgba(255,255,255,0.4); }
    .btn { width:100%; padding:15px; background:linear-gradient(90deg,#11998e,#38ef7d); color:white; border:none; border-radius:14px; font-size:16px; font-weight:700; cursor:pointer; margin-top:8px; }
    .link { text-align:center; margin-top:20px; color:rgba(255,255,255,0.6); font-size:14px; }
    .link a { color:#a78bfa; font-weight:700; text-decoration:none; }
  </style>
</head>
<body>
<div class="card">
  <div class="logo-circle">✨</div>
  <div class="app-name">SocialApp</div>
  <div class="app-tagline">Join Our Community</div>
  <% if (error) { %><div class="error">⚠️ <%= error %></div><% } %>
  <form method="POST" action="/signup">
    <div class="input-group">
      <span>👤</span>
      <input type="text" name="username" placeholder="Username choose karo" required>
    </div>
    <div class="input-group">
      <span>🔒</span>
      <input type="password" name="password" placeholder="Password banao" required>
    </div>
    <button type="submit" class="btn">CREATE ACCOUNT</button>
  </form>
  <div class="link">Already have account? <a href="/login">Login</a></div>
</div>
<script>
if ('serviceWorker' in navigator) navigator.serviceWorker.register('/sw.js');
</script>
</body>
</html>
